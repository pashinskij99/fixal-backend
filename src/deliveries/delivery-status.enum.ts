export enum DeliveryStatus {
  DRAFT = 'draft',
  CREATED = 'created',
  IN_TRANSIT = 'in_transit',
  ARRIVED = 'arrived',
  PICKED_UP = 'picked_up',
  RETURNED = 'returned',
  CANCELLED = 'cancelled',
}
